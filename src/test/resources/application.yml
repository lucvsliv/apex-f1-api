spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/apexf1db
    username: apexuser
    password: apexpass
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      embedding:
#        model: text-embedding-3-small